{
  "hooks": {
    "PreCompact": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "echo '=== workflow-state.json (teammates spawned — NE PAS PERDRE) ===' && cat .claude/workflow-state.json 2>/dev/null || echo '{\"teammates\":[]}' ; touch .claude/.post-compact"
          }
        ]
      }
    ],
    "UserPromptSubmit": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "[ -f .claude/.post-compact ] && echo '=== RESTAURATION POST-COMPACTAGE — teammates spawned ===' && cat .claude/workflow-state.json 2>/dev/null && rm -f .claude/.post-compact || true"
          }
        ]
      }
    ]
  }
}
